<UserControl x:Class="AutoReportWizard.Views.Step6View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutoReportWizard"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBoolConverter x:Key="InvBool"/>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,32">
            <TextBlock Text="Output &amp; Generate" Foreground="{StaticResource TextPrimaryBrush}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" TextWrapping="Wrap" Text="Select your output folder and generate the final T-SQL and RDLC output files."/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Inputs and Log -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="24" Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="REPORT METADATA" Foreground="{StaticResource AccentBrush}" FontWeight="Medium" FontSize="12" Margin="0,0,0,16"/>
                        
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Report Name" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding ReportName, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Output Folder" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center"/>
                            <Button Grid.Column="3" Content="Browse..." Style="{StaticResource DarkBtn}" Click="BrowseFolder_Click" Padding="16,8" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="GENERATION LOG" Foreground="{StaticResource TextSecondaryBrush}" FontWeight="Medium" FontSize="11" Margin="0,0,0,8"/>
                        <ScrollViewer Grid.Row="1" x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                            <TextBox x:Name="LogBox" Text="{Binding GenerationLog}" Background="Transparent" Foreground="#34D399" FontFamily="Consolas" FontSize="13" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right: Action Panel -->
            <Border Grid.Column="2" Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="16" Padding="32">
                <StackPanel VerticalAlignment="Center">
                    <Button x:Name="GenerateBtn" Content="GENERATE REPORT" Style="{StaticResource NeoPillButton}" Height="56" FontSize="14" FontWeight="Bold" IsEnabled="{Binding IsGenerating, Converter={StaticResource InvBool}}" Click="GenerateBtn_Click" Margin="0,0,0,24">
                        <Button.Effect>
                            <DropShadowEffect Color="#7C3AED" BlurRadius="25" ShadowDepth="0" Opacity="0.5"/>
                        </Button.Effect>
                    </Button>

                    <StackPanel Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}}">
                        <ProgressBar IsIndeterminate="True" Height="4" Background="#0F172A" Foreground="{StaticResource AccentBrush}" BorderThickness="0" Margin="0,0,0,16"/>
                        <TextBlock Text="Processing Phase A &amp; B..." Foreground="{StaticResource AccentGoldBrush}" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                    </StackPanel>

                    <Border Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="PHASE A" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                            <TextBlock x:Name="PhaseALabel" Text="T-SQL Generator" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="Medium"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="{StaticResource WindowBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16">
                        <StackPanel>
                            <TextBlock Text="PHASE B" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                            <TextBlock x:Name="PhaseBLabel" Text="RDLC XML Engine" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="Medium"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>