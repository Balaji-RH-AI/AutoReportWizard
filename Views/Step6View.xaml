<UserControl x:Class="AutoReportWizard.Views.Step6View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutoReportWizard"
             Background="Transparent">

        <UserControl.Resources>
                <BooleanToVisibilityConverter x:Key="BoolToVis"/>
                <local:InverseBoolConverter x:Key="InvBool"/>
        </UserControl.Resources>

        <Grid>
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Output &amp; Generate"
                           Foreground="White"
                           FontSize="26"
                           FontWeight="Bold"/>

                <TextBlock Grid.Row="1"
                           Foreground="Gray"
                           FontSize="13"
                           Margin="0,5,0,16"
                           TextWrapping="Wrap"
                           Text="Select your output folder and generate the final T-SQL and RDLC output files."/>

                <StackPanel Grid.Row="2"
                                Margin="0,0,0,14">

                        <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="Report Name:"
                                           Foreground="#D4AF37"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1"
                                         Text="{Binding ReportName, UpdateSourceTrigger=PropertyChanged}"
                                         Background="#1A1A1A"
                                         Foreground="White"
                                         BorderBrush="#444"
                                         Padding="8"
                                         FontSize="12"/>
                        </Grid>

                        <Grid>
                                <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="Output Folder:"
                                           Foreground="#D4AF37"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1"
                                         Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}"
                                         Background="#1A1A1A"
                                         Foreground="White"
                                         BorderBrush="#444"
                                         Padding="8"
                                         FontSize="12"
                                         VerticalAlignment="Center"/>
                                <Button Grid.Column="3"
                                        Content="Browse..."
                                        Style="{StaticResource DarkBtn}"
                                        Click="BrowseFolder_Click"
                                        VerticalAlignment="Center"/>
                        </Grid>
                </StackPanel>

                <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="#0D1117"
                                BorderBrush="#333"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10">
                                <ScrollViewer x:Name="LogScroller"
                                              VerticalScrollBarVisibility="Auto">
                                        <TextBox x:Name="LogBox"
                                                 Text="{Binding GenerationLog}"
                                                 Background="Transparent"
                                                 Foreground="#00FF88"
                                                 FontFamily="Consolas"
                                                 FontSize="12"
                                                 BorderThickness="0"
                                                 IsReadOnly="True"
                                                 TextWrapping="Wrap"
                                                 AcceptsReturn="True"/>
                                </ScrollViewer>
                        </Border>

                        <StackPanel Grid.Column="2"
                                    VerticalAlignment="Center"
                                    MinWidth="180">
                                <Button x:Name="GenerateBtn"
                                        Content="Generate Output"
                                        Style="{StaticResource GoldBtn}"
                                        IsEnabled="{Binding IsGenerating, Converter={StaticResource InvBool}}"
                                        Click="GenerateBtn_Click"/>

                                <TextBlock Text="Processing..."
                                           Foreground="#D4AF37"
                                           HorizontalAlignment="Center"
                                           Margin="0,16,0,0"
                                           Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}}"/>

                                <TextBlock x:Name="PhaseALabel"
                                           Text="Phase A - T-SQL"
                                           Foreground="#555"
                                           FontSize="12"
                                           Margin="0,20,0,6"
                                           HorizontalAlignment="Center"/>

                                <TextBlock x:Name="PhaseBLabel"
                                           Text="Phase B - RDLC XML"
                                           Foreground="#555"
                                           FontSize="12"
                                           HorizontalAlignment="Center"/>
                        </StackPanel>
                </Grid>
        </Grid>
</UserControl>