<UserControl x:Class="AutoReportWizard.Views.Step6View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AutoReportWizard"
             xmlns:wf="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBoolConverter x:Key="InvBool"/>

        <Style x:Key="GoldBtn"
                TargetType="Button">
            <Setter Property="Background"
                    Value="#D4AF37"/>
            <Setter Property="Foreground"
                    Value="#0A0A0A"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="Padding"
                    Value="18,9"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter Property="Background"
                                        Value="#B8962A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                    Value="False">
                                <Setter Property="Opacity"
                                        Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Live RDLC Preview"
                   Foreground="White"
                   FontSize="26"
                   FontWeight="Bold"/>

        <TextBlock Grid.Row="1"
                   Foreground="Gray"
                   FontSize="13"
                   Margin="0,5,0,16"
                   TextWrapping="Wrap"
                   Text="Enter parameter values and run a Visual Studio-style rendered preview of your RDLC layout."/>

        <Grid Grid.Row="2"
                Margin="0,0,0,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#111111"
                    BorderBrush="#333"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ScrollViewer Grid.Column="0"
                            HorizontalScrollBarVisibility="Auto"
                            VerticalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding DynamicParameters}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,20,0"
                                            VerticalAlignment="Bottom">
                                        <StackPanel Orientation="Horizontal"
                                                Margin="0,0,0,4">
                                            <TextBlock Text="{Binding PromptText}"
                                                    Foreground="#D4AF37"
                                                    FontSize="12"
                                                    FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ParameterName, StringFormat=' ({0})'}"
                                                    Foreground="#777"
                                                    FontSize="11"
                                                    Margin="4,0,0,0"
                                                    VerticalAlignment="Bottom"/>
                                        </StackPanel>
                                        <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                 MinWidth="140"
                                                 MaxWidth="220"
                                                Background="#1A1A1A"
                                                Foreground="White"
                                                 BorderBrush="#444"
                                                Padding="8,5"
                                                FontSize="12"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Bottom"
                            Margin="15,0,0,0">
                        <TextBlock Text="{Binding PreviewError}"
                                Foreground="#FF7777"
                                FontSize="11"
                                TextWrapping="Wrap"
                                   Margin="0,0,15,0"
                                VerticalAlignment="Center"
                                MaxWidth="200"
                                   Visibility="{Binding HasPreviewError, Converter={StaticResource BoolToVis}}"/>

                        <Button Content="Run Preview"
                                Style="{StaticResource GoldBtn}"
                                Command="{Binding RunPreviewCommand}"
                                IsEnabled="{Binding IsPreviewRunning, Converter={StaticResource InvBool}}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="2"
                    Background="#2D2D30"
                    BorderBrush="#333"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <wf:WindowsFormsHost x:Name="ReportHost"
                                         Visibility="{Binding HasPreviewData, Converter={StaticResource BoolToVis}}"/>

                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasPreviewData}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Click Run Preview to render the RDLC report layout."
                                   Foreground="#888"
                                   FontSize="14"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="The preview uses your column headers, widths, and page header configuration."
                                   Foreground="#555"
                                   FontSize="12"
                                   Margin="0,8,0,0"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
